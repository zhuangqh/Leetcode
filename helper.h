#ifndef HELPER_H
#define HELPER_H

#include <iostream>
#include <vector>

using namespace std;

// method to test
class helper {
 public:
  vector<int> vectorGenerator(int len);
  void printVector(vector<int>& vec);
};

#endif
